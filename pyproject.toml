[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hdb-resale-tech-test"
version = "1.0.0"
description = "HDB resale flat prices ETL technical test submission"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "Pillow>=10.0",
    "jupyter>=1.0",
]

[tool.setuptools.packages.find]
where = ["src"]
